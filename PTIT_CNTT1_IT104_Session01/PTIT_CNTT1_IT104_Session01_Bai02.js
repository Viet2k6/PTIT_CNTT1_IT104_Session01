const a = 5;
const arr = [1, 2, 3];
a = 6; 
arr.push(4); 

// Biến được khai báo bằng const không thể thay đổi giá trị của nó, nhưng nếu là mảng hoặc đối tượng thì có thể thay đổi nội dung bên trong.